# Telegram Bot Settings (Global)
TELEGRAM_TOKEN=""

# WhatsApp HTTP API (WAHA) Settings (Global)
WAHA_BASE_URL="http://localhost:5000"
WAHA_SESSION="default"
WAHA_API_KEY="secretkey"

# App PIN (4 digit)
APP_PIN="1234"

# Port Configuration
FRONTEND_PORT=3000
BACKEND_PORT=8000

# CORS allowed origins (comma-separated). "*" allows all but disables credentials.
CORS_ORIGINS="*"
